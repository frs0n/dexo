What to Test

- Forum list now supports manual reordering — tap "Edit" on the top left, then drag the right-edge grabber to reorder. Order is persisted across launches
- Fix poll voting not refreshing the UI: selected option and updated tally now appear immediately after voting (single or multiple choice)
